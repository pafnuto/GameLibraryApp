import React, { useState } from "react";
import { GameTypes } from "../../GameTypes";


const GameList = () => {
    return (
        <div>
            
        </div>
    );
};

export default GameList;