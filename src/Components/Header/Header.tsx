import { Component } from "react";
import { Link } from "react-router-dom";

interface IHeaderProps {
    activeTab: string;
    pathName: string;
    handleActiveTab: boolean;
  }

  
const Header = () => {
    return (
        switch (key) {
            case value:
                
                break;
        
            default:
                break;
        }
    );
};

export default Header;